import React from 'react';
import useTitle from '../Title/UseTitle';

const Home = () => {
    useTitle("Home")
    return (
        <div>
            Home
        </div>
    );
};

export default Home;